#!/usr/bin/env bash
# Apply pending numbered migrations (schema only) against the DB. The demo seed is
# NO LONGER a migration — use bin/seed (dev) to lay in the base cast.
set -euo pipefail
cd "$(dirname "$0")/.."

cd server && gleam run -m tempo/migrate
